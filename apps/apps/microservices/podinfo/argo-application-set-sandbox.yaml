apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: podinfo-sandbox
  namespace: argocd
spec:
  generators:
    - matrix:
        generators:
          - git:
              repoURL: git@github.com:templarfelix/akuity-appofapps.git
              revision: HEAD
              files:
              - path: apps/_cluster_config/sandbox/**/**/config.yaml
              pathParamPrefix: target
          - git:
              repoURL: git@github.com:templarfelix/akuity-appofapps.git
              revision: HEAD
              files:
              - path: apps/_dataset_config/sandbox/config.yaml
              pathParamPrefix: app
  template:
    metadata:
      name: 'podinfo-{{values.suffix}}'
    spec:
      project: "microservices"
      source:
        repoURL: git@github.com:templarfelix/akuity-appofapps.git
        targetRevision: HEAD
        path: apps/apps/microservices/podinfo/data/default
      destination:
        name: '{{server}}'
        namespace: microservices
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
          allowEmpty: false
        managedNamespaceMetadata:
          labels:
            istio-injection: enabled
        syncOptions:
        - CreateNamespace=true
        - PrunePropagationPolicy=foreground