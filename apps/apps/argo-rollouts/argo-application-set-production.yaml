apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: argo-rollouts-production
  namespace: argocd
spec:
  generators:
    - matrix:
        generators:
          - git:
              repoURL: git@github.com:templarfelix/akuity-appofapps.git
              revision: HEAD
              files:
              - path: apps/_cluster_config/production/**/**/config.yaml
              pathParamPrefix: target
          - git:
              repoURL: git@github.com:templarfelix/akuity-appofapps.git
              revision: HEAD
              files:
              - path: apps/_dataset_config/production/config.yaml
              pathParamPrefix: app
  template:
    metadata:
      name: 'argo-rollouts-{{values.suffix}}'
    spec:
      project: "argo-rollouts"
      source:
        repoURL: git@github.com:templarfelix/akuity-appofapps.git
        targetRevision: HEAD
        path: apps/apps/argo-rollouts/data/default
      destination:
        name: '{{server}}'
        namespace: argo-rollouts
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
          allowEmpty: false
        syncOptions:
        - CreateNamespace=true
        - PrunePropagationPolicy=foreground